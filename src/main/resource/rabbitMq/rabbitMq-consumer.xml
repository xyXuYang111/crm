<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xsi:schemaLocation="
       http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd
       http://www.springframework.org/schema/rabbit
       http://www.springframework.org/schema/rabbit/spring-rabbit.xsd">

    <context:property-placeholder location="classpath:properties/xuyang.properties" />
    <!-- 连接服务配置  -->
    <rabbit:connection-factory id="connectionFactoryRabbit" host="${rabbitMq.url}" username="${rabbitMq.userName}"
                               password="${rabbitMq.password}" port="${rabbitMq.port}"  />

    <!--通过指定下面的admin信息，当前producer中的exchange和queue会在rabbitmq服务器上自动生成 -->
    <rabbit:admin id="connectAdmin" connection-factory="connectionFactoryRabbit"/>

    <!--定义queue -->
    <!-- durable:是否持久化
        exclusive: 仅创建者可以使用的私有队列，断开后自动删除
        auto_delete: 当所有消费客户端连接断开后，是否自动删除队列 -->
    <rabbit:queue id="queue_1" name="${rabbitMq.queue_1}" durable="true" auto-delete="false" exclusive="false"/>
    <rabbit:queue id="queue_2" name="${rabbitMq.queue_2}" durable="true" auto-delete="false" exclusive="false"/>
    <rabbit:queue id="queue_3" name="${rabbitMq.queue_3}" durable="true" auto-delete="false" exclusive="false"/>

    <!-- 设置Ack模式为手动 -->
    <bean id="ackManual" class="org.springframework.beans.factory.config.FieldRetrievingFactoryBean">
        <property name="staticField" value="org.springframework.amqp.core.AcknowledgeMode.MANUAL" />
    </bean>

    <!-- 定义direct exchange，绑定queueTest -->
    <!-- 交换机的四种模式：
        direct：转发消息到 routigKey 指定的队列。
        topic：按规则转发消息（最灵活）。
        headers：（这个还没有接触到）
        fanout：转发消息到所有绑定队列 -->
    <rabbit:direct-exchange id="crm" name="crm" durable="true" auto-delete="false">
        <rabbit:bindings>
            <rabbit:binding queue="${rabbitMq.queue_1}" key="${rabbitMq.queue_key_1}"/>
            <rabbit:binding queue="${rabbitMq.queue_2}" key="${rabbitMq.queue_key_2}"/>
            <rabbit:binding queue="${rabbitMq.queue_3}" key="${rabbitMq.queue_key_3}"/>
        </rabbit:bindings>
    </rabbit:direct-exchange>

    <bean id="rabbitMqConsumer" class="com.xuyang.crm.rabbitMq.consumer.RabbitMqConsumer"/>

    <!-- queue litener  观察 监听模式 当有消息到达时会通知监听在对应的队列上的监听对象-->
    <rabbit:listener-container connection-factory="connectionFactoryRabbit" acknowledge="auto">
        <rabbit:listener queues="${rabbitMq.queue_1}" ref="rabbitMqConsumer"/>
        <rabbit:listener queues="${rabbitMq.queue_2}" ref="rabbitMqConsumer"/>
        <rabbit:listener queues="${rabbitMq.queue_3}" ref="rabbitMqConsumer"/>
    </rabbit:listener-container>
</beans>